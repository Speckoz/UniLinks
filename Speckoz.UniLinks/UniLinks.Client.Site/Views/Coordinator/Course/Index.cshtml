@model ResultModel<CourseVO>

@{
    ViewData["Title"] = "Informaçoes do Curso";
}

@if (Model != null)
{
    switch (Model.StatusCode)
    {
        case System.Net.HttpStatusCode.OK:
            break;
        case System.Net.HttpStatusCode.Created:
        case System.Net.HttpStatusCode.NoContent:
            <script>
                sendMessage("@Model.Message", "success");
            </script>
            break;
        default:
            <script>
                sendMessage("@Model.Message", "error");
            </script>
            break;
    }
}

<div class="row" style="margin-left: 0; margin-right: 0">
    <div class="col-sm-12 my-auto">
        <div class="text-center" style="padding-bottom: 2em; padding-top: 2em;">
            <span class="theme-text">Justo diam vel consetetur amet tempor amet ea quis amet facilisi tempor ut no et</span>
        </div>
        <div class="container">
            <div class="row justify-content-center">
                <div class="md-form">
                    <input type="text" id="courseName" class="form-control" value="@Model.Object.Name" readonly style="color:grey" />
                    <label for="courseName">Nome do Curso</label>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="md-form">
                    <input type="number" id="periodsNumber" class="form-control" value="@Model.Object.Periods" readonly style="color:grey" />
                    <label for="periodsNumber">Nº Periodos</label>
                </div>
            </div>
            <div class="row justify-content-center">
                <a asp-action="UpdateCourse" asp-controller="Course" class="btn btn-primary waves-effect justify-content-center">Atualizar Informaçoes</a>
            </div>
        </div>
    </div>
</div>